# Duplicate this file and rename as '.env.local' for local development.
# Configure your agent webhook URL to connect OTO Reach Agents.

# -----------------------------------------------------------------------------
# Database (Neon PostgreSQL)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@YOUR_HOST/neondb?sslmode=require

# -----------------------------------------------------------------------------
# Neon Auth
# -----------------------------------------------------------------------------
NEON_AUTH_URL=https://ep-dark-block-ahnsteqp.neonauth.c-3.us-east-1.aws.neon.tech/neondb/auth
NEON_JWKS_URL=https://ep-dark-block-ahnsteqp.neonauth.c-3.us-east-1.aws.neon.tech/neondb/auth/.well-known/jwks.json

# -----------------------------------------------------------------------------
# Agent Webhook URL:
# Set the webhook URL for your AI agent. This is where chat messages will be sent.
# -----------------------------------------------------------------------------
AGENT_WEBHOOK_URL= # <-- Insert your agent webhook URL here

# -----------------------------------------------------------------------------
# JWT Secret (for token validation)
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-here-change-in-production-min-64-chars-long

# -----------------------------------------------------------------------------
# Supabase Encryption Key (for encrypting user's Supabase credentials)
# -----------------------------------------------------------------------------
# IMPORTANT: This key is used to encrypt user's Supabase credentials (Service Role, Anon Key)
# Generate a secure 32+ character key for production
# Generate with: openssl rand -base64 32
SUPABASE_ENCRYPTION_KEY=your-supabase-encryption-key-change-in-production-32-chars-min

# -----------------------------------------------------------------------------
# Theme Configuration (optional):
# -----------------------------------------------------------------------------
NEXT_PUBLIC_DEFAULT_THEME=light
